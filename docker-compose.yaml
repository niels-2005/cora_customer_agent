services:
  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    environment:
      - CHROMA_DB_PATH=/data/chroma_db
    volumes:
      - chroma_data:/data

volumes:
  chroma_data: